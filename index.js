// npm
// reuse our own code in other projects
// use other devs' code in our projects
// share our own code with others

// package - folder that contains the shared code
//      dependency, module (different terms for the same thing)

// npm --version / npm -v   - version

// Install local dependency (will use more often)
// npm i <packageName>

// Global depencency
// npm install -g <packageName>

// Set Up package.json
// Manifest file (contains metadata and dependencies)

// npm init / npm init -y (default)